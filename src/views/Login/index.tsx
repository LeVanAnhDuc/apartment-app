// component
import LoginBlock from "./mains/LoginBlock";

const Login = () => (
  <section className="flex size-full h-screen flex-1 flex-col pt-20">
    <LoginBlock />
  </section>
);

export default Login;
